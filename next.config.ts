import type { NextConfig } from "next";

// SECTION: Next.js runtime configuration
const nextConfig: NextConfig = {
  reactStrictMode: true
};
// End of section: strict mode helps catch unsafe React usage while the API application is developed.

export default nextConfig;
